class Solution:
    def numOfStrings(self, patterns: List[str], word: str) -> int:
        c = 0
        for string in patterns:
            if string in word:
                c += 1
        return c
